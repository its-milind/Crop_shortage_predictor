import pandas as pd\nimport numpy as np\n\ndef generate_synthetic_data(samples=5000):\n    np.random.seed(0)  # For reproducibility\n    # Generating synthetic features\n    temperature = np.random.uniform(15, 40, size=samples)  # in Celsius\n    rainfall = np.random.uniform(0, 300, size=samples)  # in mm\n    soil_moisture = np.random.uniform(0, 1, size=samples)  # fractional\n    crop_yield = np.random.uniform(0, 100, size=samples)  # in tons per hectare\n\n    # Generating target variable (1 = disaster, 0 = no disaster)\n    disaster = (temperature > 35) & (rainfall < 50) & (soil_moisture < 0.3)  # simple condition\n    disaster = disaster.astype(int)\n\n    # Creating a DataFrame\n    data = pd.DataFrame({\n        'Temperature': temperature,\n        'Rainfall': rainfall,\n        'Soil Moisture': soil_moisture,\n        'Crop Yield': crop_yield,\n        'Disaster': disaster\n    })\n    return data\n\nif __name__ == '__main__':\n    dataset = generate_synthetic_data(5000)\n    dataset.to_csv('synthetic_crop_disaster_dataset.csv', index=False)\n    print('Synthetic crop disaster dataset generated!')